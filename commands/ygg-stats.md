---
description: Show what's in Yggdrasil memory (by project, type, scope)
---
Run `ygg stats` via Bash (fall back to `uvx --from yggdrasil-memory ygg stats`) and summarize what's stored: total live vs archived, and the breakdown by project, type and scope. If memory is empty, suggest `/ygg-seed` to populate it from existing work. If a scale hint is shown (store getting large), relay it.
